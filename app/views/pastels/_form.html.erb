<%= form_for(@pastel) do |f| %>
  <% if @pastel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pastel.errors.count, "error") %> prohibited this pastel from being saved:</h2>

      <ul>
      <% @pastel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
